package simone.bonvicini.travalert.travalert.alarm;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;


public class BootReceiver extends BroadcastReceiver {

    @Override
    public void onReceive(Context pContext, Intent intent) {

//        AlarmScheduler.init(pContext);
//        Preferences preferences = Preferences.getInstance(pContext);
//
//        if (preferences != null && preferences.isAlarmEnabled()) {
//
//            AlarmScheduler.get().setAlarms(Preferences.getInstance(pContext).getAlarms());
//        }
    }
}